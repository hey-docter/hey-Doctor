<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heydoctor.app.mapper.QnaMapper">
    <select id="selectList" resultType="qnaDTO">
        SELECT Q.QNA_ID, U.USER_ID, QNA_TITLE, QNA_CONTENT, QNA_REGISTER_DATETIME, QNA_IS_PUBLIC, DOCTOR_DEPARTMENT_TYPE, U.USER_NAME
        FROM TBL_USER U JOIN TBL_QNA Q
        ON U.USER_ID = Q.USER_ID

--         <![CDATA[WHERE ROWNUM > (#{page, jdbcType=VARCHAR} * 10) AND ROWNUM <= (#{page, jdbcType=VARCHAR} * 10 + 10)]]>
<!--        <choose>-->
<!--            <when test="search != null">-->
<!--                AND FALSE-->
<!--            </when>-->
<!--        </choose>-->
    </select>

    <select id="selectOne" resultType="qnaDTO">
        SELECT QNA_ID, USER_ID, QNA_TITLE, QNA_CONTENT, QNA_REGISTER_DATETIME, QNA_IS_PUBLIC, DOCTOR_DEPARTMENT_TYPE,
         U.USER_NAME
        FROM TBL_USER U JOIN TBL_QNA Q
        ON U.USER_ID = Q.USER_ID AND Q.QNA_ID = #{qnaId, jdbcType=VARCHAR}
    </select>

    <insert id="insert">
<!--        <selectKey keyProperty="qnaId" order="BEFORE" resultType="qnaVO">-->
<!--            SELECT SEQ_QNA.NEXTVAL FROM DUAL-->
<!--        </selectKey>-->
        INSERT INTO TBL_QNA (
        QNA_ID, USER_ID, QNA_TITLE, QNA_CONTENT, QNA_IS_PUBLIC, DOCTOR_DEPARTMENT_TYPE
        )
        VALUES (
        SEQ_QNA.NEXTVAL, #{userId, jdbcType=VARCHAR}, #{qnaTitle, jdbcType=VARCHAR}, #{qnaContent, jdbcType=VARCHAR},
        #{qnaIsPublic, jdbcType=VARCHAR}, #{doctorDepartmentType, jdbcType=VARCHAR}
        )
    </insert>

    <delete id="delete">
        DELETE FROM TBL_QNA WHERE QNA_ID = #{qnaId, jdbcType=VARCHAR}
    </delete>
</mapper>