<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heydoctor.app.mapper.AnswerMapper">
    <insert id="insert">
        INSERT INTO TBL_ANSWER (
                ANSWER_ID, USER_ID, QUESTION_ID, ANSWER_TITLE,
                ANSWER_CONTENT, ANSWER_REGISTER_DATETIME, ANSWER_IS_PUBLIC, ANSWER_LIKE_COUNT,
                ANSWER_IS_ADOPTED
        )
        VALUES (
                #{answerId}, #{userId}, #{questionId}, #{answerTitle},
                #{answerContent}, #{answerRegisterDateTime}, #{answerIsPublic}, #{answerLikeCount},
                #{answerIsAdopted}
       );
    </insert>

    <select id="selectByQuestionId" resultType="answerVO">
        SELECT ANSWER_ID, USER_ID, QUESTION_ID, ANSWER_TITLE,
               ANSWER_CONTENT, ANSWER_REGISTER_DATETIME, ANSWER_IS_PUBLIC, ANSWER_LIKE_COUNT,
               ANSWER_IS_ADOPTED
        FROM TBL_ANSWER <![CDATA[WHERE QUESTION_ID = #{questionId}]]>
    </select>
</mapper>